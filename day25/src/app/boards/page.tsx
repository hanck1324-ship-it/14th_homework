
// 1. "게시글 목록" 컴포넌트를 불러옵니다. (컴포넌트 이름은 보통 대문자로 시작하는 것이 좋습니다.)
import Boardslistpage from "components/boards-list";

export default function BoardsPage() { 
      return(
    // 2. 불러온 "게시글 목록" 컴포넌트를 화면에 그려줍니다.
    <Boardslistpage />
    )
}
